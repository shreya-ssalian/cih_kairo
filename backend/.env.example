# Comma separated list of allowed origins, or "*" for any (fine for local dev).
SEAPULSE_CORS_ORIGINS=http://localhost:5173

# Simulation defaults - can also be changed at runtime via /api/simulator/*.
SEAPULSE_TICK_RATE_MS=800
SEAPULSE_SHIP_TARGET=1000
